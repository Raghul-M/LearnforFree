# YouTube API Configuration
VITE_YOUTUBE_API_KEY=your_youtube_api_key_here

# Instructions to get your YouTube API key:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Navigate to "APIs & Services" → "Library"
# 4. Search for "YouTube Data API v3" and enable it
# 5. Go to "APIs & Services" → "Credentials"
# 6. Click "Create Credentials" → "API Key"
# 7. Copy the generated API key and replace "your_youtube_api_key_here"
# 8. (Optional) Restrict the API key to YouTube Data API v3 for security

# Setup Instructions:
# 1. Copy this file to .env (already done)
# 2. Replace "your_youtube_api_key_here" with your actual API key
# 3. Never commit the .env file to version control
